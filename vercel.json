{
  "buildCommand":"cd client && npm i && npm run build",
  "outputDirectory":"client/dist",
  "framework":"vite",
  "rewrites":[{
    "source":"/admin/(.*)",
    "destination":"/server/app.js"
  }]
}